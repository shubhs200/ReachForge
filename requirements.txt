# ReachForge - LLM-powered Fuzzing Tool for OSS-Fuzz Libraries

# Core dependencies
requests>=2.31.0,<3

# Libclang bindings for callgraph analysis (pinned for compatibility)
libclang==14.0.6

# OpenAI LLM adapter (optional)
# openai>=1.0.0